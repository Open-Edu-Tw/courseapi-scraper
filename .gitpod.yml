image:
  file: .gitpod.Dockerfile

tasks:
  - name: Install dependencies
    command:
      pdm install
  - name: Start MongoDB
    command:
      mongod

vscode:
  extensions:
    - "ms-python.python"
    - "bungcip.better-toml"
